events {
  worker_connections  1024;
}

http {
  server {
    listen 80;
    server_name myalovitsa.stud.shpp.me;

    location ~ /.well-known/acme-challenge {
      allow all;
      root /var/www/html;
    }

    location / {
      proxy_pass http://api:3005/;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
  }

  # server {
  #   listen 443;
  #   server_name myalovitsa.stud.shpp.me;

  #   ssl_certificate /etc/letsencrypt/myalovitsa.stud.shpp.me/fullchain.pem;
  #   ssl_certificate_key /etc/letsencrypt/myalovitsa.stud.shpp.me/privkey.pem;

  #   location / {
  #     proxy_pass https://api:3005/;
  #     proxy_set_header Host $host;
  #     proxy_set_header X-Real-IP $remote_addr;
  #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
  #   }
  # }
}
